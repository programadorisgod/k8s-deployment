apiVersion: v1
kind: Service
metadata:
  labels:
    app: swyw-svc-auth
  name: swyw-svc-auth
  namespace: swyw
spec:
  ports:
    - name: auth-pod
      port: 4000
      protocol: TCP
      targetPort: 4000
  selector:
    app: swyw-auth
  type: ClusterIP
