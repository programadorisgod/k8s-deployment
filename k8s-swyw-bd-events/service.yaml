# postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: database-events
  namespace: postgres
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5433
      targetPort: 5433
